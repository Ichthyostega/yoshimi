From: Ichthyostega <prg@ichthyostega.de>
Date: Fri, 6 Sep 2019 06:25:33 +0200
Subject: compile-fix for older GCC

basically yoshimi requires C++11,
however, the support for this new language standard was somehow brittle
with the compilers GCC 4.7 and 4.8

In this case here, the new-style initialiser syntax caused the parsing to flounder.
Using parenthesis instead of curly braces for the initialisation of the reference
fixes this problem
---
 src/CLI/CmdInterpreter.cpp | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)

Index: b/src/CLI/CmdInterpreter.cpp
===================================================================
--- a/src/CLI/CmdInterpreter.cpp
+++ b/src/CLI/CmdInterpreter.cpp
@@ -103,7 +103,7 @@ CmdInterpreter::CmdInterpreter() :
     currentInstance{0},
     synth{nullptr},
     instrumentGroup{},
-    textMsgBuffer{TextMsgBuffer::instance()},
+    textMsgBuffer(TextMsgBuffer::instance()),
 
     context{LEVEL::Top},
     npart{0},
